package comparison;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class LengthTest {
    @Test
    public void shouldCompareTwoLengths() {
        Length lengthInFt = new Length(1);
        Length lengthInInch = new Length(12);
//        assertEquals(Comparison.EQUAL, lengthInFt.compare())
    }
}
